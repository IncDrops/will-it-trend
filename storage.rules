rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Disallow all reads and writes by default.
    // The application does not use Firebase Storage, so access is disabled for security.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
